{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "Build site",
      "detail": "Rebuilds the static site into public/ (Live Server serves this folder; rebuild after source edits).",
      "type": "shell",
      "command": "npm run build",
      "group": {
        "kind": "build",
        "isDefault": true
      },
      "problemMatcher": []
    }
  ]
}
