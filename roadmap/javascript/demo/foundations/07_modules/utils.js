// utils.js — a module: it exports abilities that other files import.
// Anything NOT exported stays private to this file.

export function greet(name) {
  return `Hello, ${name}!`; // template literal — lesson 06 (strings)
}
